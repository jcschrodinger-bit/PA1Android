<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icono_elegance"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icono_elegance"
        android:supportsRtl="true"
        android:theme="@style/Theme.PA1Android">

        <activity
            android:name=".ui.activities.SplashScreenActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activities.MainActivity" android:exported="false" />
        <activity android:name=".ui.activities.CategoriasActivity" android:exported="false" />
        <activity android:name=".ui.productos.ProductosActivity" android:exported="false" />
        <activity android:name=".ui.auth.TerminosActivity" android:exported="false" />
        <activity android:name=".ui.activities.ComprasActivity" android:exported="false" />
        <activity android:name=".ui.activities.ProductoDetalleActivity" android:exported="false" />
        <activity android:name=".ui.activities.PerfilActivity" android:exported="false" />
        <activity android:name=".ui.activities.CheckoutSuccessActivity" android:exported="false" />

    </application>

</manifest>
