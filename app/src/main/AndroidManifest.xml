<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icono_elegance"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icono_elegance"
        android:supportsRtl="true"
        android:theme="@style/Theme.PA1Android">

        <activity
            android:name=".SplashScreenActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".TerminosActivity" android:exported="false" />
        <activity android:name=".CheckoutSuccessActivity" android:exported="false" />

        <activity android:name=".pages.tienda.TiendaActivity" android:exported="false" />
        <activity android:name=".pages.productos.ProductosActivity" android:exported="false" />
        <activity android:name=".pages.compras.ComprasActivity" android:exported="false" />
        <activity android:name=".pages.Perfil.PerfilActivity" android:exported="false" />
        <activity android:name=".pages.productodetalle.ProductoDetalleActivity" android:exported="false" />

    </application>

</manifest>
